apiVersion: v1
kind: Secret
metadata:
  name: reservoir-ai-secrets
  namespace: reservoir-ai
type: Opaque
data:
  # Encode with: echo -n 'value' | base64
  MLFLOW_USERNAME: # base64 encoded
  MLFLOW_PASSWORD: # base64 encoded
  MINIO_ACCESS_KEY: # base64 encoded
  MINIO_SECRET_KEY: # base64 encoded
  DATABASE_URL: # base64 encoded
